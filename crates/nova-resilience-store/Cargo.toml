[package]
name = "nova-resilience-store"
edition.workspace = true
version.workspace = true

[dependencies]
async-trait.workspace = true
redis = { workspace = true, optional = true, features = ["aio", "tokio-comp"] }

[features]
default = []
redis-store = ["redis"]
