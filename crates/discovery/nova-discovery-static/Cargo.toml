[package]
name = "nova-discovery-static"
version = "0.1.0"
edition.workspace = true

[dependencies]
nova-core = { path = "../../nova-core" }
async-trait.workspace = true
tokio = { workspace = true, features = ["sync", "rt", "macros"] }
